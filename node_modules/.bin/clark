#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../clark/bin/clark.js" "$@"
  ret=$?
else 
  node  "$basedir/../clark/bin/clark.js" "$@"
  ret=$?
fi
exit $ret
